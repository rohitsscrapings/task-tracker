import React from "react";
import ReactDOM from "react-dom/client";
import App from "./final-project/App.jsx";
import "./final-project/styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
